# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ArduPilot source clone created by ardupilot_build/build.sh (multi-GB)
ardupilot_build/ardupilot/

# OS / editor
.DS_Store
*.swp

# scratch
scratchpad/
*.log

# Large yolov8m detector: kept local (99 MB each), not in git. The nano
# models/visdrone_n.onnx is the in-repo default; use the bigger one with
# --detector models/visdrone_m.onnx. Add via Git LFS if you want it tracked.
models/visdrone_m.onnx
models/visdrone_m.mlpackage/
# raw Colab training run dumped into the repo (weights already copied to models/)
visdrone_m/

# generated demo clip
demo_aerial.mp4

demo_car_follow.mp4
