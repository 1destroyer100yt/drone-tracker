# Pinned to the versions verified working for this project.
# NOTE: opencv-python 5.x dropped the classic detectors and changed APIs;
# 4.12 is the tested line. On 64-bit Raspberry Pi OS these wheels install as-is.

opencv-python==4.12.0.88
mediapipe==0.10.35
pymavlink==2.4.49
flask==3.1.3
scipy>=1.10        # used by the vendored DroTrack object tracker (third_party/)
onnxruntime>=1.16  # optional: --detector (detection-assisted, drift-proof follow)

# numpy is pulled in by opencv/mediapipe; left unpinned so their resolver
# picks a compatible build.
